import React from "react";
import styles from "./MovieList.module.scss";

const MovieList = ({ title }) => {
  return <div className={styles["movie-container"]}>MovieList</div>;
};

export default MovieList;
